import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Dark Music Theme Colors
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color card = Color(0xFF282828);

  // Brand / Accent (Spotify Green style)
  static const Color primary = Color(0xFF1DB954);
  static const Color primaryLight = Color(0xFF1ED760);
  static const Color secondary = Color(0xFFBB86FC);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textMuted = Color(0xFF757575);

  // Status & Error
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);

  // Dividers & Borders
  static const Color divider = Color(0xFF2C2C2C);
  static const Color bottomNavBg = Color(0xFF181818);
}
